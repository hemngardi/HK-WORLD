<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE html>
<html b:css='false' b:defaultwidgetversion='2' b:layoutsVersion='3' expr:dir='data:blog.languageDirection' expr:lang='data:blog.locale' xmlns='http://www.w3.org/1999/xhtml' xmlns:b='http://www.google.com/2005/gml/b' xmlns:data='http://www.google.com/2005/gml/data' xmlns:expr='http://www.google.com/2005/gml/expr'>
<head>
    <meta content='width=device-width, initial-scale=1' name='viewport'/>
    <title><data:blog.title/></title>
    
    <!-- Slider CSS -->
    <b:skin><![CDATA[
    body {
      background-color: #000000;
      margin: 0;
      padding: 0;
      color: #ffffff;
    }
    
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 15px;
    }
    
    .header {
      padding: 20px 0;
      background: linear-gradient(45deg, #1a1a1a, #000);
      text-align: center;
      border-bottom: 3px solid #333;
    }

    /* Anime Slider Styles */
    .anime-slider {
      position: relative;
      width: 100%;
      height: 400px;
      margin: 20px 0;
      overflow: hidden;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0,0,0,0.5);
    }

    .slide {
      position: absolute;
      width: 100%;
      height: 100%;
      opacity: 0;
      transition: all 0.8s ease;
      background-size: cover;
      background-position: center;
    }

    .slide.active {
      opacity: 1;
    }

    .slider-nav {
      position: absolute;
      bottom: 20px;
      left: 50%;
      transform: translateX(-50%);
      z-index: 10;
    }

    .slider-dot {
      width: 12px;
      height: 12px;
      margin: 0 8px;
      border-radius: 50%;
      background: rgba(255,255,255,0.5);
      display: inline-block;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .slider-dot.active {
      background: #fff;
      transform: scale(1.2);
    }

    .slider-arrow {
      position: absolute;
      top: 50%;
      transform: translateY(-50%);
      width: 40px;
      height: 40px;
      background: rgba(0,0,0,0.5);
      border-radius: 50%;
      cursor: pointer;
      z-index: 10;
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-size: 24px;
      transition: all 0.3s ease;
    }

    .slider-arrow:hover {
      background: rgba(0,0,0,0.8);
    }

    .slider-prev {
      left: 20px;
    }

    .slider-next {
      right: 20px;
    }

    .slide-content {
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      padding: 20px;
      background: linear-gradient(transparent, rgba(0,0,0,0.8));
      color: white;
    }
    
    .post {
      margin-bottom: 30px;
      background: rgba(26, 26, 26, 0.8);
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.5);
    }

    .footer {
      text-align: center;
      padding: 20px 0;
      background: linear-gradient(45deg, #000, #1a1a1a);
      border-top: 3px solid #333;
      margin-top: 40px;
    }

    .header h1 {
      font-size: 2.5em;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
      margin: 0;
    }

    a {
      color: #4a9eff;
      text-decoration: none;
      transition: color 0.3s ease;
    }

    a:hover {
      color: #66b3ff;
    }
    ]]></b:skin>

    <b:template-skin>
      <![CDATA[
        .post-body img {
          max-width: 100%;
          height: auto;
          border-radius: 8px;
          box-shadow: 0 2px 5px rgba(0,0,0,0.5);
        }
      ]]>
    </b:template-skin>

    <!-- Slider JavaScript -->
    <script>
    //<![CDATA[
    document.addEventListener('DOMContentLoaded', function() {
      const slides = document.querySelectorAll('.slide');
      const dots = document.querySelectorAll('.slider-dot');
      let currentSlide = 0;

      function showSlide(n) {
        slides[currentSlide].classList.remove('active');
        dots[currentSlide].classList.remove('active');
        
        currentSlide = (n + slides.length) % slides.length;
        
        slides[currentSlide].classList.add('active');
        dots[currentSlide].classList.add('active');
      }

      // Next/Previous controls
      document.querySelector('.slider-next').addEventListener('click', () => {
        showSlide(currentSlide + 1);
      });

      document.querySelector('.slider-prev').addEventListener('click', () => {
        showSlide(currentSlide - 1);
      });

      // Dot navigation
      dots.forEach((dot, index) => {
        dot.addEventListener('click', () => {
          showSlide(index);
        });
      });

      // Auto advance slides
      setInterval(() => {
        showSlide(currentSlide + 1);
      }, 5000);

      // Show first slide
      showSlide(0);
    });
    //]]>
    </script>
</head>

<body>
  <div class='container'>
    <!-- Header -->
    <header class='header'>
      <b:section id='header'>
        <b:widget id='Header1' locked='true' title='Blog Title' type='Header' version='2'>
          <b:widget-settings>
            <b:widget-setting name='displayUrl'/>
            <b:widget-setting name='displayHeight'>0</b:widget-setting>
            <b:widget-setting name='sectionWidth'>-1</b:widget-setting>
            <b:widget-setting name='useImage'>false</b:widget-setting>
            <b:widget-setting name='shrinkToFit'>false</b:widget-setting>
            <b:widget-setting name='imagePlacement'>BEHIND</b:widget-setting>
            <b:widget-setting name='displayWidth'>0</b:widget-setting>
          </b:widget-settings>
          <b:includable id='main' var='this'>
            <h1><data:title/></h1>
            <b:include name='description'/>
          </b:includable>
        </b:widget>
      </b:section>
    </header>

    <!-- Anime Slider -->
    <div class='anime-slider'>
      <div class='slide active' style='background-image: url("https://via.placeholder.com/1200x400/1a1a1a/ffffff?text=Anime+Slide+1")'>
        <div class='slide-content'>
          <h2>Latest Anime Updates</h2>
          <p>Discover new releases and trending series</p>
        </div>
      </div>
      <div class='slide' style='background-image: url("https://via.placeholder.com/1200x400/1a1a1a/ffffff?text=Anime+Slide+2")'>
        <div class='slide-content'>
          <h2>Featured Reviews</h2>
          <p>Read our latest anime reviews</p>
        </div>
      </div>
      <div class='slide' style='background-image: url("https://via.placeholder.com/1200x400/1a1a1a/ffffff?text=Anime+Slide+3")'>
        <div class='slide-content'>
          <h2>Upcoming Events</h2>
          <p>Stay updated with anime events and releases</p>
        </div>
      </div>

      <div class='slider-nav'>
        <span class='slider-dot active'></span>
        <span class='slider-dot'></span>
        <span class='slider-dot'></span>
      </div>

      <div class='slider-arrow slider-prev'>&#10094;</div>
      <div class='slider-arrow slider-next'>&#10095;</div>
    </div>

    <!-- Main Content -->
    <main class='main'>
      <b:section class='main' id='main'>
        <b:widget id='Blog1' locked='true' title='Blog Posts' type='Blog' version='2'>
          <b:widget-settings>
            <b:widget-setting name='showDateHeader'>true</b:widget-setting>
            <b:widget-setting name='style.textcolor'>#ffffff</b:widget-setting>
            <b:widget-setting name='showShareButtons'>true</b:widget-setting>
            <b:widget-setting name='showCommentLink'>true</b:widget-setting>
            <b:widget-setting name='style.urlcolor'>#ffffff</b:widget-setting>
            <b:widget-setting name='showAuthor'>true</b:widget-setting>
            <b:widget-setting name='style.linkcolor'>#ffffff</b:widget-setting>
            <b:widget-setting name='showLabels'>true</b:widget-setting>
            <b:widget-setting name='showLocation'>false</b:widget-setting>
            <b:widget-setting name='showTimestamp'>true</b:widget-setting>
            <b:widget-setting name='postsPerAd'>1</b:widget-setting>
            <b:widget-setting name='showBacklinks'>true</b:widget-setting>
            <b:widget-setting name='style.bgcolor'>#000000</b:widget-setting>
            <b:widget-setting name='showInlineAds'>false</b:widget-setting>
            <b:widget-setting name='showReactions'>false</b:widget-setting>
          </b:widget-settings>
        </b:widget>
      </b:section>
    </main>

    <!-- Footer -->
    <footer class='footer'>
      <p>Copyright © <data:blog.title/> <script>document.write(new Date().getFullYear())</script></p>
    </footer>
  </div>
</body>
</html>
